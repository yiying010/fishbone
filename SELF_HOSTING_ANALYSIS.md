# 魚骨洞天自架伺服器部署分析

更新日期：2026-08-12

## 摘要結論

本專案**可以自架**，但應分成兩個層次看待。

| 部署內容 | 自架可行性 | 建議 |
| --- | --- | --- |
| `docs/` GitHub Pages 範例 | 高 | 可直接放到任何靜態網頁伺服器；最適合目前的公開活動展示。 |
| `public/fishbone.html` 單一活動頁 | 高 | 可直接由 Nginx、Apache、Caddy 或學校伺服器提供。 |
| `app/` 完整 vinext 應用 | 可行，但需調整與驗證 | 目前的建置流程以 Cloudflare Workers 為主要目標；若要用 VPS／校內伺服器，應先建立 Node.js 版產物並進行測試。 |

因此，若目標是先讓學生或訪客能使用「魚骨洞天」，優先選擇 GitHub Pages 或一般靜態主機最合適。若未來需要帳號、跨裝置保存資料、教師後台等功能，再規劃完整應用的自架。

## 本專案現況

- 前端採用 React、Next.js API 介面與 vinext。
- `app/page.tsx` 目前以 iframe 載入 `/fishbone.html`；主要活動內容可獨立作為靜態網頁使用。
- `docs/index.html` 是可直接發布到 GitHub Pages 的簡化範例。
- `.openai/hosting.json` 中 D1 與 R2 均為 `null`，表示目前沒有啟用資料庫或檔案儲存桶。
- 專案雖保留 ChatGPT 登入輔助程式，但目前首頁沒有使用它。
- `worker/index.ts` 使用 Cloudflare Worker 的 `ASSETS` 與影像處理介面，代表完整應用的目前設定偏向 Cloudflare 執行環境。

## 方案比較

### 方案 A：GitHub Pages／靜態主機（目前建議）

**適用情境**：公開教學活動、活動介紹、學生自行在瀏覽器操作，且不需要伺服器保存答案。

**部署內容**：`docs/`，或未來整理成一份獨立的靜態網站資料夾。

**優點**：成本低、維護量小、沒有伺服器更新與資料庫管理；推送到 GitHub 後可自動更新。

**限制**：無法安全保存班級資料、無法建立真正的登入機制，也不適合處理私密或個人資料。

### 方案 B：校內／自有伺服器的靜態網站

**適用情境**：學校已有網站空間或 VPS，並希望使用自己的網域，例如 `fishbone.school.edu.tw`。

**部署內容**：同方案 A 的靜態檔案。

**需求**：HTTPS 憑證、網域 DNS 設定，以及能上傳 HTML、CSS、JavaScript 檔案的網站空間。

**優點**：可使用自有網域與校方既有管理流程；技術風險低。

**限制**：仍然是靜態網站；資料不應只寫入伺服器檔案，若要保存學生資料仍需新增後端與資料庫。

### 方案 C：自架完整 vinext 應用

**適用情境**：未來新增教師帳號、學生作品保存、資料查詢、上傳檔案或需要伺服器端權限控管時。

**需求**：

- Node.js 22.13 或更新版本。
- Linux VPS 或校內伺服器、反向代理（Nginx／Caddy 等）與 HTTPS。
- 程序管理（Docker Compose、systemd 或同類工具），讓服務重開機後自動啟動。
- 自架資料庫（建議 PostgreSQL）與備份機制；若要上傳檔案，還需要物件儲存或受管理的檔案空間。

**必要調整**：目前專案的 Vite 設定與 `worker/index.ts` 以 Cloudflare Workers 為主。採用 Node.js 自架時，應另建立 Node 部署設定，使用 vinext 的 Node/standalone 輸出，並逐項驗證路由、圖片處理與環境變數。不要直接假設目前的 `npm run start` 可以在一般 VPS 上作為正式服務。

**風險**：vinext 的主要部署目標是 Cloudflare Workers，且仍在快速演進中；完整自架需要在正式上線前做相容性與負載測試。[vinext 官方專案](https://github.com/cloudflare/vinext)

## 建議的實施順序

1. 先發布 `docs/` 到 GitHub Pages，確認教學流程、文案與介面。
2. 若需要自有網域，將同一份靜態檔案移至校內網站空間或 VPS。
3. 確認真的需要「保存資料、登入、教師後台」後，再把完整應用改為 Node.js 可執行版本，並加入資料庫、備份與權限設計。

## 資安與個資提醒

- 不要將 API 金鑰、帳號密碼或學生個資提交到 GitHub；`.env*` 已被 `.gitignore` 排除。
- 公開 GitHub Pages 不適合放名冊、可識別個人的作品或教師帳密。
- 若未來保存學生資料，應先決定資料保存期限、可存取人員、備份方式與刪除流程。
- 自架伺服器應定期更新系統與 Node.js，並強制使用 HTTPS。

## 最終建議

目前以**靜態部署**最符合專案功能與維護成本：GitHub Pages 可作為公開示範網址；若校方要求自有網域，直接把同一份靜態網站部署到校內伺服器即可。完整 vinext 版本可自架，但應視為下一階段的工程工作，而非直接複製現有檔案就能完成的部署。
